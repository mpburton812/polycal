<?xml version="1.0" encoding="utf-8"?>
<!-- RemoteViews-safe: TextView + ImageView with shape drawable (no EditText / vector). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:contentDescription="@string/widget_nlp_description"
    android:orientation="vertical"
    android:padding="8dp">

    <TextView
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="@string/widget_nlp_label"
        android:paddingBottom="4dp"
        android:text="@string/widget_nlp_label"
        android:textColor="@color/garden_surface"
        android:textSize="12sp"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_input"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:background="@drawable/widget_field_background"
            android:contentDescription="@string/widget_nlp_hint"
            android:gravity="center_vertical"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="@string/widget_nlp_hint"
            android:textColor="@color/garden_ink_muted"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/widget_send"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/widget_send_icon"
            android:contentDescription="@string/widget_send"
            android:gravity="center"
            android:text="›"
            android:textColor="@color/garden_ink"
            android:textSize="22sp"
            android:textStyle="bold" />
    </LinearLayout>
</LinearLayout>
