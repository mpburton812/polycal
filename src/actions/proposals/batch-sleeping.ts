/**
 * Batch sleeping draft persistence hooks used during draft submit (PC-62).
 * Implementation lives in ./_core (persistBatchSleepingDraft); this module documents the domain split.
 */
export type { BatchSleepingEntry } from "@/lib/proposals/batch-sleeping";
