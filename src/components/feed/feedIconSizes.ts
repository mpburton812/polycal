/**
 * Shared parrot icon sizes for bottom nav vs feed like controls (PC-270, PC-277).
 * Nav parrot matches MUI BottomNavigationAction default (~24px).
 */
export const BOTTOM_NAV_ICON_PX = 24;

/** Feed like (upvote) bird — 3× the prior half-nav size (12 → 36) for tap visibility (PC-277). */
export const FEED_LIKE_BIRD_PX = 36;
