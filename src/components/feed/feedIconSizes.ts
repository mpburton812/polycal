/**
 * Shared parrot icon sizes for bottom nav vs feed like controls (PC-270).
 * Nav parrot matches MUI BottomNavigationAction default (~24px); like birds are half.
 */
export const BOTTOM_NAV_ICON_PX = 24;

/** Feed like (upvote) bird — half the bottom-nav parrot so nav reads 2× larger. */
export const FEED_LIKE_BIRD_PX = BOTTOM_NAV_ICON_PX / 2;
